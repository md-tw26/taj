# تاج (TAJ) — تحليل تقني شامل للمشروع

> **تاريخ التحليل:** 2026-09-08
> **مسار المشروع:** `C:\Users\MSI\Desktop\taj`
> **منهجية التحقق:** كل رقم في هذا التقرير مأخوذ من تشغيل فعلي على هذا الجهاز — `flutter analyze` و `flutter test` و قراءة مباشرة للملفات. ما لم أستطع إثباته مكتوب صراحةً على أنه *غير مُتحقَّق منه*.
> **البيئة:** Flutter 3.29.3 · Dart 3.7.2 · Windows 11. (لا يوجد Python/PostgreSQL مطلوب — المشروع Flutter خالص.)

---

## ⚠️ 0. تنبيه مهم: التقرير القديم لا يخص هذا المشروع

يوجد تقرير سابق بعنوان *"TAJ Comprehensive Technical Analysis Report"* يصف مشروعًا مكوّنًا من **Django + PostgreSQL + Riverpod + drift + dio + معمارية offline-first + مخطط قاعدة بيانات 74 جدولًا + مسارات Linux مثل `/mnt/user-data/outputs/`**.

**هذا الوصف لا ينطبق إطلاقًا على الكود الموجود فعليًا على القرص.** المشروع الحقيقي هنا هو **تطبيق Flutter خالص** لا يحتوي على Django ولا PostgreSQL ولا Riverpod ولا drift ولا dio ولا أي backend. التقرير القديم كان عن لقطة/مشروع مختلف تمامًا، ويجب تجاهله عند الحديث عن هذا المجلد. هذا التقرير يحلّل **ما هو موجود فعلًا الآن.**

---

## 1. ملخص تنفيذي

**تاج** تطبيق **نقاط بيع (POS) عربي RTL** مبني بـ Flutter، موجّه للسوق الليبي (عطور، بخور، هدايا، ملابس)، مستوحى من برامج مثل "المحاسب 3". هو **نموذج/واجهة عاملة (prototype)** متقدّمة وذات جودة عالية، لكنه يعمل على **بيانات تجريبية في الذاكرة** محفوظة محليًا عبر `shared_preferences` — **لا يوجد خادم أو قاعدة بيانات أو شبكة.**

| المؤشر | القيمة (مُتحقَّق منها) |
|---|---|
| نوع المشروع | تطبيق Flutter (POS/ERP) — واجهة فقط، بلا backend |
| عدد ملفات Dart في `lib/` | 30 ملفًا |
| أسطر الكود في `lib/` | **18,263 سطرًا** |
| ملفات الاختبار | 6 ملفات (512 سطرًا) |
| `flutter analyze` | **24 ملاحظة، صفر أخطاء** (0 errors) |
| `flutter test` | **194 ناجح / 4 فاشل** |
| نقطة الدخول `main.dart` | ✅ موجودة وتعمل |
| الحالة العامة | **يُصرَّف ويعمل** — نموذج واجهة ناضج على بيانات وهمية |

**الخلاصة بسطر:** تطبيق واجهة POS عربي مكتمل بصريًا وعالي الجودة، **يُصرَّف بلا أخطاء وتنجح 98% من اختباراته**، لكنه بلا طبقة خلفية حقيقية — أي أنه *واجهة جاهزة تنتظر منطق أعمال وربط خادم فعلي.*

---

## 2. التقنيات والاعتماديات

**اللغة/الإطار:** Flutter · Dart (SDK `^3.7.2`) · Material 3 · واجهة عربية RTL كاملة.

**الاعتماديات الفعلية (من `pubspec.yaml`) — قليلة ومتعمّدة:**

| الحزمة | الغرض |
|---|---|
| `flutter` + `flutter_localizations` | الإطار + تعريب |
| `cupertino_icons ^1.0.8` | أيقونات |
| `google_fonts ^6.2.1` | خطوط (Reem Kufi / Cairo) |
| `intl (any)` | تنسيق أرقام/تواريخ عربية |
| `audioplayers ^6.1.0` | صوت تأكيد البيع |
| `shared_preferences ^2.5.3` | حفظ البيانات والتفضيلات محليًا |
| `flutter_lints ^5.0.0` (dev) | قواعد الجودة |

**ما هو غائب تمامًا (بخلاف التقرير القديم):** لا `flutter_riverpod`، لا `go_router`، لا `drift`/`sqlite`، لا `dio`/شبكة، لا `mobile_scanner`، لا `fl_chart`. إدارة الحالة والتنقل والرسوم كلها **مبنية يدويًا**.

**المنصات المهيّأة:** android · ios · linux · macos · windows · web (سكافولد متعدد المنصات كامل).

---

## 3. البنية وأحجام الملفات

```
lib/
├── main.dart                         (152)  نقطة الدخول + توجيه الأدوار
├── screenshot_main.dart              (43)   نقطة دخول بديلة للقطات/العرض
├── core/
│   ├── format.dart                   (25)   تنسيق الدينار والأرقام العربية
│   ├── sale_sound.dart               (99)   صوت البيع (مقاوم للفشل)
│   ├── user_role.dart                (64)   الأدوار والصلاحيات
│   ├── responsive.dart               (216)  نقاط الكسر التجاوبية
│   ├── branches.dart                 (17)   نموذج الفروع
│   ├── chart_style.dart              (14)   نمط الرسم (area/line/bar)
│   ├── theme/ (taj_colors 276, app_themes 286)   الألوان والثيمات
│   └── demo/ (demo_store 645, demo_models 449, demo_provider 19)  طبقة البيانات
├── shared/widgets/
│   ├── app_shell.dart                (1208) الهيكل + التنقل + لوحة الأوامر
│   ├── taj_ui.dart                   (527)  مكوّنات واجهة معاد استخدامها
│   ├── taj_table.dart                (173)  جدول موحّد
│   └── taj_filters.dart              (222)  فلاتر موحّدة
└── modules/
    ├── auth/login_screen.dart        (588)  تسجيل الدخول
    ├── cashier_pos/                          واجهة الكاشير
    │   ├── cashier_pos_screen.dart   (4558) ← أكبر ملف في المشروع
    │   └── payment_services_panel.dart (607)
    ├── merchant_pos/merchant_pos_screen.dart (523)
    ├── pos/pos_screen.dart           (699)  ⚠️ كود ميت (غير مستخدم)
    ├── pos_management/pos_management_screen.dart (2449) مراقبة نقاط البيع
    ├── dashboard/dashboard_screen.dart (580)
    ├── products/products_screen.dart (933)
    ├── inventory/inventory_screen.dart (604)
    ├── sales/sales_screen.dart       (401)
    ├── purchases/purchases_screen.dart (431)
    ├── expenses/expenses_screen.dart (436)
    ├── customers/customers_screen.dart (652)
    └── settings/settings_screen.dart (367)
```

**ملاحظة على البنية:** التنظيم نظيف ومنطقي (`core` / `shared` / `modules`)، مع مكوّنات معاد استخدامها فعلًا (`taj_ui`, `taj_table`, `taj_filters`, `app_shell`). ملف الكاشير (4558 سطرًا) ضخم جدًا ومرشّح للتقسيم.

---

## 4. نقطة الدخول والتوجيه حسب الدور

`main.dart` يشغّل `TajApp` (StatefulWidget) الذي يوفّر `DemoStore` عبر `DemoStoreProvider` (InheritedWidget)، ثم يوجّه حسب الدور:

| الدور | الشاشة |
|---|---|
| `cashier` | `CashierPosScreen` — واجهة الكاشير (بسيط/احترافي) |
| `merchant` | `MerchantPosScreen` — واجهة التاجر |
| `admin` | `AppShell` — هيكل ERP الكامل بكل الوحدات الإدارية |
| (غير مسجّل) | `LoginScreen` |

التطبيق يدعم `MaterialApp` بثيم فاتح/داكن، لون رئيسي (`TajSwatch`) قابل للتبديل، ولغة `ar` مع RTL كامل. توجد "خطاطيف اختبار" (`testSignedIn`, `testRole`, `testInitialCart`, `testOpenReports`, `initialDark`) تُستخدم في الاختبارات ولقطات الشاشة.

---

## 5. طبقة البيانات — `DemoStore`

القلب البياني للتطبيق: `DemoStore extends ChangeNotifier` (`lib/core/demo/demo_store.dart`, 645 سطرًا).

- **المصدر:** بيانات مبذورة `_seed()` (4 فروع، 12 منتجًا، 4 عملاء، 3 موردين، مبيعات/مشتريات/مصروفات/أجهزة/إشعارات/مستخدمين نموذجية).
- **الاستمرارية:** تُحفظ/تُقرأ كـ JSON عبر `shared_preferences` (`_save`/`load`/`_decode`)، مفتاح `taj_demo_store_v1`.
- **النماذج (`demo_models.dart`):** `DemoBranch, DemoProduct, DemoCustomer, DemoSupplier, DemoSale, DemoSaleLine, DemoPurchase, DemoExpense, DemoTerminal, DemoNotification, DemoUser, DemoStatusHistory`.
  - طرق الدفع: `cash, bank, card, sadad, credit`
  - حالات الفاتورة: `paid, credit, returned, pending, cancelled`
- **منطق الأعمال الموجود فعلًا:** `addSale` (تحقق من المخزون، تحديث رصيد العميل الآجل، توليد إشعار)، `adjustStock`، `upsertProduct/Customer`، `addSupplier`، `updateSaleStatus` (+ سجل حالات)، `addExpense`، `addPurchase`.
- **مؤشرات محسوبة (KPIs):** `lowStockCount, salesTotal, orderCount, unreadNotifications, lastSevenDaySales, productSales, salesForBranch, terminalsForBranch`.

**تقييم:** الطبقة مكتوبة جيدًا وبتحقق منطقي معقول (رمي `StateError` عند سلة فارغة/مخزون غير كافٍ/فرع غير موجود). لكنها **في الذاكرة بالكامل** — لا مزامنة، لا مستخدمون متعددون، لا مصدر حقيقة خارجي. مناسبة للعرض والاختبار فقط.

---

## 6. الوحدات والشاشات — حقيقية / وهمية / ميتة

**التوجيه داخل `AppShell._pageFor()`:**

| عنصر التنقل | الحالة |
|---|---|
| لوحة التحكم `dashboard` | ✅ شاشة حقيقية |
| نقاط البيع `pos` → `PosManagementScreen` | ✅ حقيقية |
| الأصناف `items` → `ProductsScreen` | ✅ حقيقية |
| المخزون والجرد `inventory` | ✅ حقيقية |
| المبيعات `sales` | ✅ حقيقية |
| المشتريات `purchases` | ✅ حقيقية |
| المصروفات `expenses` | ✅ حقيقية |
| العملاء والموردون `customers` | ✅ حقيقية |
| الإعدادات `settings` | ✅ حقيقية |
| الإشعارات `inbox` | 🟡 `_ComingSoon` (شاشة "قريبًا") |
| اليوم التشغيلي `day` | 🟡 `_ComingSoon` |
| الخزائن والبنوك `treasury` | 🟡 `_ComingSoon` |
| المحاسبة `accounting` | 🟡 `_ComingSoon` |

- **9 شاشات إدارية حقيقية** + شاشتا POS (كاشير/تاجر) + تسجيل الدخول = **12 واجهة عاملة.**
- **4 عناصر تنقل** ما تزال شاشات "قريبًا" (الإشعارات، اليوم التشغيلي، الخزائن، المحاسبة).
- **كود ميت مؤكَّد:** `lib/modules/pos/pos_screen.dart` (699 سطرًا) — الصنف `PosScreen` **غير مُشار إليه من أي مكان** (التطبيق يستخدم `CashierPosScreen`/`MerchantPosScreen`/`PosManagementScreen` بدلًا منه). مرشّح للحذف.
- علامات "قريبًا" أخرى: بعض خدمات الدفع في `payment_services_panel.dart` (5 مواضع) والإنجليزية في الإعدادات.

---

## 7. المصادقة والصلاحيات

- **تسجيل الدخول وهمي (تطويري):** أي كلمة مرور غير فارغة تُقبل، والدور يُشتق من اسم المستخدم (`admin`/`merchant`/`cashier`). موثّق في `LOGIN_CREDENTIALS.md` و README على أنه **للتطوير فقط لا للإنتاج.**
- **الصلاحيات (`user_role.dart`):** `UserPermissions { changePrice, applyDiscounts, refund, viewReports }` تُبنى عبر `UserPermissions.forRole()`:
  - `admin` و `merchant` = كل الصلاحيات.
  - `cashier` = كلها `false` (يرى واجهة POS البسيطة، بلا تعديل أسعار/خصم/استرداد/تقارير).
- **الحماية:** الصلاحيات تُطبَّق على مستوى الواجهة (إخفاء/تعطيل عناصر). **لا يوجد توكن ولا جلسة ولا خادم مصادقة** — لأن لا backend أصلًا.

---

## 8. التنقل والواجهة والتصميم

- **التنقل:** `AppShell` مخصّص (بلا `go_router`) — قائمة جانبية/شريط مصغّر/تنقل سفلي حسب المقاس، **لوحة أوامر (Command Palette)** واختصارات لوحة مفاتيح (`G D`, `G I`...).
- **التجاوب (Responsive):** نقاط كسر في `core/responsive.dart`، ومجموعة اختبارات تتحقق من عدم وجود overflow عبر ~15 مقاسًا من الهاتف حتى 4K.
- **RTL/التعريب:** `Locale('ar')`، `flutter_localizations`، اتجاه عربي كامل. الإنجليزية مُعلَنة في `supportedLocales` لكنها فعليًا "قريبًا".
- **الثيم:** `taj_colors.dart` (نظام Tokens للألوان + swatch رئيسي قابل للاختيار)، `app_themes.dart` (فاتح/داكن)، خطوط عبر `google_fonts`، أنماط رسوم بيانية (area/line/bar) مبنية يدويًا.
- **الصوت:** `SaleSound` مقاوم للفشل (يُلغى بهدوء على Linux/غياب GStreamer).

---

## 9. نتائج التحليل الساكن — `flutter analyze`

**النتيجة: 24 ملاحظة، صفر أخطاء (0 errors).** لا شيء يمنع التصريف.

- **2 تحذير (warning):** عنصران غير مستخدمين في `cashier_pos_screen.dart` — `_lowStockProducts` (سطر 4437) و `_mockCustomers` (سطر 4457).
- **22 معلومة (info):** موزّعة على نوعين:
  - `use_build_context_synchronously` — استخدام `BuildContext` بعد `await` (13+ موضعًا في customers/expenses/inventory/products/purchases/sales/cashier). **يستحق المراجعة** لأنه مصدر أخطاء محتمَل وقت التشغيل.
  - `curly_braces_in_flow_control_structures` — جُمَل `if` بلا أقواس (تجميلي).

كلها قابلة للإصلاح بسرعة؛ لا شيء منها حرج.

---

## 10. نتائج الاختبارات — `flutter test`

**النتيجة: 194 ناجح / 4 فاشل.** (ملفات الاختبار: `widget_test, probe_test, cashier_redesign_test, pos_management_test, responsive_test, chart_style_test`.)

الاختبارات فعليًا **ترسم الشاشات (render)** عبر مقاسات كثيرة، لذا نجاحها دليل قوي على أن الواجهة تعمل. الفشل الأربعة كلها **RenderFlex overflow** (تجاوز التخطيط) عند مقاسات صغيرة/ضيقة:

| # | الاختبار الفاشل | السبب |
|---|---|---|
| 1 | `professional mode — narrow: cart sheet header + toolbar, no overflow` | تجاوز في ورقة السلة (الوضع الاحترافي، عرض ضيق) |
| 2 | `simple mode — narrow: cart sheet renders without overflow` | تجاوز في ورقة السلة (الوضع البسيط، عرض ضيق) |
| 3 | `TajApp shell — no overflow ... admin @ tablet-sm (600×960)` | تجاوز **58px** من الأسفل في هيكل الأدمن |
| 4 | `TajApp shell — no overflow ... merchant @ phone-xs-landscape (568×320)` | تجاوز **135px** من اليمين في واجهة التاجر |

هذه **أخطاء تخطيط تجاوبي عند حواف المقاسات الصغيرة**، وليست أخطاء منطق. (ملاحظة داخلية سابقة ذكرت "فشلين"؛ العدد الآن **4** — أي أن الانحدار البصري اتّسع قليلًا ويجب معالجته.)

---

## 11. المشاكل مرتبة حسب الأولوية

🔴 **لا توجد مشاكل حرجة تمنع التشغيل** — التطبيق يُصرَّف ويعمل.

🟠 **عالية**
1. **لا يوجد backend/قاعدة بيانات/شبكة إطلاقًا** — كل شيء بيانات وهمية في الذاكرة. هذا أكبر فجوة نحو منتج حقيقي.
2. **تسجيل الدخول وهمي** (أي كلمة مرور تُقبل) — غير صالح للإنتاج بتاتًا.
3. **4 اختبارات فاشلة** (تجاوز تخطيط عند المقاسات الصغيرة) — تحتاج إصلاح تجاوبي.

🟡 **متوسطة**
4. تحذيرات `use_build_context_synchronously` (13+ موضعًا) — خطر أخطاء وقت تشغيل عند العمليات غير المتزامنة.
5. **كود ميت:** `pos/pos_screen.dart` (699 سطرًا) بلا مرجع — يُحذف.
6. `cashier_pos_screen.dart` ضخم (4558 سطرًا) — صعب الصيانة، يحتاج تقسيمًا.
7. 4 عناصر تنقل ما تزال "قريبًا" (الإشعارات، اليوم التشغيلي، الخزائن، المحاسبة).

🟢 **منخفضة**
8. عنصران غير مستخدمين (`_lowStockProducts`, `_mockCustomers`).
9. جُمَل `if` بلا أقواس (تجميلي).
10. الإنجليزية معلَنة كلغة مدعومة لكنها غير مُنفَّذة.

---

## 12. ما هو مبني / ناقص / وهمي

**مبني ويعمل فعلًا:**
- نقطة دخول + توجيه أدوار + ثيم فاتح/داكن + RTL عربي كامل.
- واجهة كاشير غنية جدًا (بسيط/احترافي: تعليق فاتورة، دفع مقسّم، خصم، ملاحظة، ربط عميل، إدارة وردية، تقارير جلسة).
- 9 شاشات إدارية (لوحة تحكم، أصناف، مخزون، مبيعات، مشتريات، مصروفات، عملاء/موردون، مراقبة نقاط بيع، إعدادات).
- طبقة بيانات مع منطق (بيع، مخزون، آجل، مصروفات، مشتريات، سجل حالات) + حفظ محلي.
- تنقل متقدّم (لوحة أوامر، اختصارات، تجاوب من الهاتف إلى 4K).
- 194 اختبارًا ناجحًا.

**وهمي/تجريبي (يحتاج استبدالًا للإنتاج):**
- المصادقة (أي كلمة مرور).
- البيانات (مبذورة في الذاكرة + shared_preferences، لا قاعدة بيانات).

**ناقص تمامًا:**
- أي backend / API / قاعدة بيانات / مزامنة / مستخدمون حقيقيون.
- الشاشات الأربع "قريبًا" (الإشعارات، اليوم التشغيلي، الخزائن، المحاسبة).
- طباعة إيصالات حقيقية، تكامل باركود بجهاز، تصدير PDF/Excel، دعم إنجليزي.

---

## 13. التقييم بالدرجات (0–100) — للحالة الفعلية الحالية

| المجال | الدرجة | الأساس |
|---|---:|---|
| **قابلية التصريف/التشغيل** | 92 | 0 أخطاء، `main.dart` يعمل، التطبيق يُبنى ويُشغَّل |
| **جودة الكود** | 78 | تنظيم نظيف، مكوّنات معاد استخدامها، لكن ملف كاشير ضخم + تحذيرات context |
| **الواجهة/تجربة الاستخدام** | 80 | تصميم عربي RTL متماسك، نظام ألوان، تجاوب واسع، لوحة أوامر |
| **الاختبارات** | 74 | 194/198 ناجح (98%)، لكن 4 فشل تخطيط + لا اختبارات منطق backend |
| **البنية المعمارية** | 70 | فصل core/shared/modules واضح؛ حالة يدوية بسيطة تكفي للحجم الحالي |
| **المعالجة والأداء** | 65 | لم يُقَس بأداة profiler؛ لا مشاكل ظاهرة، الاختبارات سريعة |
| **إدارة الحالة** | 62 | `ChangeNotifier`+`InheritedWidget`+`setState` — كافٍ الآن، قد يضيق مع النمو |
| **المصادقة والأمان** | 20 | وهمية بالكامل (تطويرية) — لا شيء منها للإنتاج |
| **الطبقة الخلفية/البيانات** | 12 | لا backend؛ بيانات في الذاكرة فقط |
| **الجاهزية للنشر (إنتاج)** | 25 | يُبنى لكل المنصات، لكن بلا خادم/مصادقة/بيانات حقيقية = نموذج لا منتج |

> هذه الدرجات تقيس **الواقع التشغيلي الحالي**. كـ *نموذج واجهة (UI prototype)*، الدرجات في الواجهة/التصريف/الجودة ممتازة؛ كـ *منتج POS جاهز للإنتاج*، ينقصه كامل الطبقة الخلفية والمصادقة الحقيقية.

---

## 14. التوصيات — الخطوات التالية (بالأولوية)

1. **إصلاح الاختبارات الأربعة الفاشلة** (تجاوز التخطيط عند المقاسات الصغيرة) — أسرع مكسب لإعادة الاختبارات إلى الأخضر.
2. **معالجة تحذيرات `use_build_context_synchronously`** (حراسة `mounted` بعد `await`) — تمنع أخطاء وقت تشغيل مستقبلية.
3. **حذف الكود الميت** `pos/pos_screen.dart` وتنظيف العنصرين غير المستخدمين.
4. **تقرير مصير الشاشات الأربع "قريبًا"** (بناؤها أو إخفاء عناصر تنقلها مؤقتًا).
5. **القرار الاستراتيجي الأكبر:** ربط backend حقيقي (خادم + قاعدة بيانات + مصادقة فعلية) واستبدال `DemoStore` بطبقة بيانات موصولة — هذه هي القفزة من *نموذج* إلى *منتج*.
6. عند النمو: التفكير في تقسيم `cashier_pos_screen.dart` وربما تبنّي حل حالة أقوى.

---

## 15. خلاصة بسطر واحد

> **تاج تطبيق واجهة POS عربي عالي الجودة، يُصرَّف بلا أخطاء وتنجح 98% من اختباراته على بيانات تجريبية محلية — إنه *نموذج واجهة ناضج وجاهز*، تنقصه فقط طبقة خلفية ومصادقة حقيقيتان ليصبح منتجًا فعليًا.**

*تحليل فقط — لم تُعدَّل أو تُحذَف أي ملفات في المشروع. المصدر: `flutter analyze` + `flutter test` + قراءة مباشرة، على `C:\Users\MSI\Desktop\taj` بتاريخ 2026-09-08.*
